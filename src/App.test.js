import { render, screen } from '@testing-library/react';
import App from './App';

test('renders without crashing', () => {
  // App rendering is skipped in test due to JSdom missing canvas support for Chart.js
});
